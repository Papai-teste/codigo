'use strict';

module.exports = function (app) {
  /*
    #swagger.path = '/v1/consentimento/solicitar/{cpf}'
    #swagger.parameters['cpf'] = { 
        in: 'path',
        description: "CPF do cidadão que será solicitado o consentimento",
        required: true
    }

    #swagger.requestBody = {
      required: true,
      description: 'Informações da solicitação do consentimento',
      content: {
        "application/json": {
          schema: {
            "$ref": "#/components/schemas/bodyConsentimento"
          }
        }
      }
    }

    #swagger.security = [{
        "bearerAuth": []
    }]
    
    #swagger.responses[200] = { 
      description: 'Consentimento solicitado com sucesso.', 
      schema: { "idConsentimento": "62be0d8a7c1e0f19b33639e5" }
    }
    #swagger.responses[400] = { description: 'Bad Request' }
    #swagger.responses[401] = { description: 'Unauthorized' }
    #swagger.responses[404] = { description: 'Registro não encontrado na base' }
    #swagger.responses[422] = { description: 'Unprocessable Entity' }
    #swagger.responses[429] = { description: 'Too Many Requests' }
    #swagger.responses[500] = { description: 'Internal Server Error' }
    #swagger.responses[502] = { description: 'Erro em alguma das APIs consumidas pelo GaaP' }
  */
};
